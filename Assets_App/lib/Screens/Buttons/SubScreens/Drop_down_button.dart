import 'package:flutter/material.dart';

class DropDownButtons extends StatelessWidget {
  const DropDownButtons({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Drop Down Button Widget'),
        centerTitle: true,
      ),
    );
  }
}
