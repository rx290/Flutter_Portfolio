import 'package:flutter/material.dart';

class RawImages extends StatelessWidget {
  const RawImages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('RawImage Widget'),
        centerTitle: true,
      ),
    );
  }
}
