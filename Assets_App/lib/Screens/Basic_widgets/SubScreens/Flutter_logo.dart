import 'package:flutter/material.dart';

class FlutterLogos extends StatelessWidget {
  const FlutterLogos({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('FlutterLogo Widget'),
        centerTitle: true,
      ),
    );
  }
}
