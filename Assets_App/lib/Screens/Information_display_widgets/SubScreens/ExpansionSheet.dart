import 'package:assets_app/supporting_libraries/libraries.dart';

class ExpansionSheets extends StatelessWidget {
  const ExpansionSheets({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Expansion Sheet Widget'),
        centerTitle: true,
      ),
    );
  }
}
