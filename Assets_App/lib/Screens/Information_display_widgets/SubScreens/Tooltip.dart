import 'package:assets_app/supporting_libraries/libraries.dart';

class Tooltips extends StatelessWidget {
  const Tooltips({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tooltips Widget'),
        centerTitle: true,
      ),
    );
  }
}
