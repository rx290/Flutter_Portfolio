import 'package:assets_app/supporting_libraries/libraries.dart';

class ListBodies extends StatelessWidget {
  const ListBodies({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List Body Widget'),
        centerTitle: true,
      ),
    );
  }
}
