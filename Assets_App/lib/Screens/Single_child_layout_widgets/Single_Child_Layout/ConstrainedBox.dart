import 'package:flutter/material.dart';

class ConstrainedBoxs extends StatelessWidget {
  const ConstrainedBoxs({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Constrained Box Widget'),
        centerTitle: true,
      ),
    );
  }
}
