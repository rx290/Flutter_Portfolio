import 'package:assets_app/supporting_libraries/libraries.dart';

class OffStages extends StatelessWidget {
  const OffStages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('OffStage Widget'),
        centerTitle: true,
      ),
    );
  }
}
